#Instrucciones Pipenv (entorno virtual)

Ingresar a carpeta "Backend"
```sh
cd Backend
```

Instalar entorno virtual
```sh
pipenv install -r requirements.txt
```

Activar entorno virtual (shell)
```sh
pipenv shell
```

Ejecutar comandos desde el exterior del entorno
```sh
pipenv run
```





